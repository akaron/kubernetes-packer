apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: prometheus
  labels:
    app: prometheus
spec:
  rules:
  - host: prometheus.localk8s
    http:
      paths:
      - path: /
        backend:
          serviceName: prometheus-server
          servicePort: 80

